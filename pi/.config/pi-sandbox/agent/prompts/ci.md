---
description: Diagnose a failing CI pipeline and propose the smallest correct fix
argument-hint: "<run URL or log>"
---
Diagnose the following CI failure: $@

Use the ci-failure-investigator skill. Classify the failure type, pinpoint the root cause, and propose the smallest targeted fix. Do not touch unrelated code.
