# Global Agent Instructions

- When something is ambiguous, ask one focused question.
- After changes, run the lightest relevant validation.
- Use `fd` (fd-find) instead of `find`, and `rg` (ripgrep) instead of `grep`, for file/content searches.
- Never run `git commit` or `git push` (or any equivalent) unless I have explicitly said so in that moment. Staging/diffing/status checks are fine; committing or pushing requires my clear, explicit go-ahead each time.
