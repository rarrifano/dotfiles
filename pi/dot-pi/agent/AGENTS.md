# Global Agent Instructions

- When something is ambiguous, ask one focused question.
- After changes, run the lightest relevant validation.
- Never run `git commit` or `git push` (or any equivalent) unless I have
  explicitly said so in that moment. Staging/diffing/status checks are fine;
  committing or pushing requires my clear, explicit go-ahead each time.
